RFC2136 section 3.2.1 states the usage of YXRRSET. YXRRSET must be returned when a record with the
same name  and type  exists, and the prerequisites require the name to not exist.
The test also checks to see if the code stops processing after the YXRRSET.